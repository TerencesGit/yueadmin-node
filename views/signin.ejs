<!DOCTYPE html>
<html lang="en">
<head>
	<%- include('link') %>
  <link rel="stylesheet" href="/css/login.css">
</head>
<body>
  <div class="container animated fadeInDown">
    <h2 class="page-header"><%= title %></h2>
    <form id="signinForm" action="/user/signin" method="post">
      <div class="form-group">
        <% if(user && user.error){%>
        <p class="alert alert-danger"><i class="fa fa-warning"></i><%= user.error%></p>
        <% } %>
        <input type="text" name="user[username]" id="username" class="form-control" placeholder="邮箱/手机号" value="<%if(user){%><%= user.username%><%}%>" required />
      </div>
      <div class="form-group">
        <input type="password" name="user[password]" id="password" class="form-control" placeholder="请输入密码" value="<%if(user){%><%= user.password%><%}%>" required />
      </div>
      <div class="form-group row">
        <div class="col-md-8">
          <input type="text" id="authcode2" name="authcode" class="form-control" placeholder="请输入验证码" required />
        </div>
        <div class="col-md-4">
          <canvas id="canvasCode" class="canvas-code" width="100%" height="35px" onClick="drawCode()"></canvas>
        </div>
      </div>
      <div class="form-group remember">
        <label class="checkbox-inline">
          <input type="checkbox" id="remember" name="user[remember]" <%if(user && user.remember){ %> checked <%}%> />
          记住密码
        </label> 
      </div>
      <button type="submit" id="btnSignIn" class="btn btn-primary btn-block">立即登录</button>
    </form>
    <div class="login-footer text-center">
      <p>
        <a href="/signup">尚未注册？</a>
        <span class="line">|</span>
        <a data-toggle="modal" href="#myModal">忘记密码</a>
      </p>
    </div>
  </div>
  <div aria-hidden="true" aria-labelledby="myModalLabel" role="dialog" tabindex="-1" id="myModal" class="modal fade">
      <div class="modal-dialog">
          <div class="modal-content">
              <div class="modal-header">
                  <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                  <h4 class="modal-title">忘记密码</h4>
              </div>
              <div class="modal-body">
                <form action="#" method="get" id="findPasswdForm">
                  <div class="form-group">
                    <label class="control-label">请输入绑定邮箱号</label>
                    <input type="text" name="email" id="bindEmail" class="form-control" placeholder="输入邮箱号">
                  </div>
                  <div class="form-group text-right">
                    <button data-dismiss="modal" class="btn btn-default" type="button">取消</button>
                    <button type="submit" id="btnFindPasswd" class="btn btn-success">提交</button>
                  </div>
                </form>
              </div>
          </div>
      </div>
  </div>
  <%- include('script')%>
  <script src="/js/jquery.cookie.js"></script>
  <script src="/js/login.js"></script>
</body>
</html>