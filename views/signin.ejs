<!DOCTYPE html>
<html lang="en">
<head>
	<%- include('link') %>
</head>
<body class="login-page">
  <div class="container animated fadeInDown">
    <form action="/user/signin" method="post" id="signinForm" class="login-form form-horizontal">
      <h2 class="page-header"><%= title %></h2>
      <% if(user && user.error){%>
      <div class="clearfix">
        <div class="alert alert-danger"><i class="fa fa-warning"></i><%= user.error%></div>
      </div>
      <% } %>
      <div class="form-group">
        <label for="username" class="control-label col-md-3">用户名</label>
        <div class="col-md-9">
          <input type="text" name="user[username]" id="username" class="form-control" placeholder="邮箱/手机号" value="<%if(user){%><%= user.username%><%}%>" required />
        </div>
      </div>
      <div class="form-group">
        <label for="password" class="control-label col-md-3">密码</label>
        <div class="col-md-9">
          <input type="password" name="user[password]" id="password" class="form-control" placeholder="请输入密码" value="<%if(user){%><%= user.password%><%}%>" required />
        </div>
      </div>
      <div class="form-group">
        <label for="authcode" class="control-label col-md-3">验证码</label>
        <div class="col-md-6">
          <input type="text" id="signinCode" name="authcode" class="form-control" placeholder="请输入验证码" required />
        </div>
        <div class="col-md-2">
          <canvas id="canvasCode" class="canvas-code" width="80px" height="34px" onClick="drawCode()"></canvas>
        </div>
      </div>
      <div class="form-group remember">
        <label class="checkbox-inline">
          <input type="checkbox" id="remember" name="user[remember]" <%if(user && user.remember){ %> checked <%}%> />
          记住密码
        </label> 
      </div>
      <div class="form-group submit">
        <button type="submit" id="btnSignIn" class="btn btn-primary btn-block">立即登录</button>
      </div>
      <div class="form-group text-center">
        <a href="/signup">尚未注册？</a>
        <span class="line">|</span>
        <a data-toggle="modal" href="/account/find_password">忘记密码</a>
      </div>
    </form>
  </div>
  <%- include('script')%>
  <script src="/js/jquery.cookie.js"></script>
  <script src="/js/login.js"></script>
</body>
</html>